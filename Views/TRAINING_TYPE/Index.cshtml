@model IEnumerable<WFPtest.Models.TRAINING_TYPE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.type_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.typ01)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.type02)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.type22)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.type_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.typ01)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.type02)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.type22)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.type_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.type_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.type_id })
        </td>
    </tr>
}

</table>
