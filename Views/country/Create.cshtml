@model WFPtest.Models.country

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>countrys</h4>
        <hr />
        @Html.ValidationSummary(true)

        <table class=" table" >
            <tr>
                <th>
            @Html.Label("Name")
                </th>
                <td>
                @Html.EditorFor(model => model.country_name)
                @Html.ValidationMessageFor(model => model.country_name)
                </td>

                <th>
                    @Html.Label("description")
                </th>
                <td>
                    @Html.EditorFor(model => model.country_description)
                    @Html.ValidationMessageFor(model => model.country_description)
                </td>
                </tr>
            </table>

        <table border="0" align="center">
            <tr>
                <td>
                    <input type="submit" value="Create" class="btn btn-default" />
                </td>
                <td>
                    @Html.ActionLink("Back to List", "Index")
                </td>
            </tr>
        </table>
    </div>
}


