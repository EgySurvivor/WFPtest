@model IEnumerable<WFPtest.Models.TRAINING_PLAN>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TRAINING_PLACE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PERIOD_FROM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PERIOD_TO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TRAINING_PLACE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PERIOD_FROM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PERIOD_TO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PLAN_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PLAN_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PLAN_ID })
        </td>
    </tr>
}

</table>
